@model Kaisokku_Common.video

@{
    /**/

    /**/

    ViewBag.Title = "VideoManagement";
}

@Html.Hidden("DeleteVideoId")
<h1>@Kaisokku_Sales.Resources.Resource.VideoManagement</h1>

@*@using (Html.BeginForm("VideoManagement", "Home", FormMethod.Post))
    {*@
<button type="submit" class="btn btn-primary btncreatemenu">@Kaisokku_Sales.Resources.Resource.CreateNewVideo</button>
@*}*@

@if (TempData["Message"] != null && TempData["Message"].ToString() != "")
{
    <div>

        @if (TempData["ErrorCode"] != null && TempData["ErrorCode"].ToString() == "1")
        {
            <div class="row">
                <div class="col-md-4" style="padding-left: 0px;padding-top: 10px;">
                    <div class="alert alert-success alert-dismissible ">
                        <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                        <strong>Success!</strong> @TempData["Message"].ToString()
                    </div>
                </div>
            </div>

        }
        else
        {
            <div class="row">
                <div class="col-md-4" style="padding-left: 0px;padding-top: 10px;">
                    <div class="alert alert-danger alert-dismissible ">
                        <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                        <strong>Danger!</strong> @TempData["Message"].ToString()
                    </div>
                </div>
            </div>

        }

    </div>

}

<div>
    <br />
    <div style="background:white;padding:10px;">
        <table id="VideoGrid" class="table table-striped table-bordered dt-responsive nowrap" width="100%" cellspacing="0" cellpadding="5">
            <thead>
                <tr>
                    <th>MediaId</th>
                    <th>@Kaisokku_Sales.Resources.Resource.Title</th>
                    <th>@Kaisokku_Sales.Resources.Resource.Description</th>
                    @*<th>Filename</th>*@
                    <th>@Kaisokku_Sales.Resources.Resource.IpAddress</th>
                    <th>@Kaisokku_Sales.Resources.Resource.ViewCount</th>
                    <th>User Type</th>
                    <th>@Kaisokku_Sales.Resources.Resource.Edit</th>
                    <th>@Kaisokku_Sales.Resources.Resource.View</th>
                    <th>@Kaisokku_Sales.Resources.Resource.Delete</th>
                </tr>
            </thead>
        </table>
    </div>
</div>
<!-- Modal CreateMenuModal -->
<div class="modal fade" id="CreateVideoModal" tabindex="-1" role="dialog" aria-labelledby="CreateVideoModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="CreateVideoModalLabel">@Kaisokku_Sales.Resources.Resource.CreateVideo</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body createVideocontent">
                CreateVideoModal

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">@Kaisokku_Sales.Resources.Resource.Close</button>
                <button type="button" class="btn btn-primary btnscreateavemenu">@Kaisokku_Sales.Resources.Resource.SaveChanges</button>
            </div>
        </div>
    </div>
</div>
<!-- Button EditMenuModal trigger modal -->
@*<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#EditMenuModal"></button>*@

<!-- Modal EditMenuModal -->
<div class="modal fade" id="EditVideoModal" tabindex="-1" role="dialog" aria-labelledby="EditVideoModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="EditVideoModalLabel">@Kaisokku_Sales.Resources.Resource.EditVideo</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body editVideocontent" style="height:750px">
                EditVideoModal

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">@Kaisokku_Sales.Resources.Resource.Close</button>
                <button type="button" class="btn btn-primary btnsavemenu">@Kaisokku_Sales.Resources.Resource.SaveChanges</button>
            </div>
        </div>
    </div>
</div>

<!-- Modal ViewMenuModal -->
<div class="modal fade" id="ViewVideoModal" tabindex="-1" role="dialog" aria-labelledby="ViewVideoModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="ViewVideoModalLabel">@Kaisokku_Sales.Resources.Resource.ViewVideo</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body ViewVideocontent">
                ViewVideoModal

            </div>
            @*<div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-primary btnsavemenu">Save changes</button>
                </div>*@
        </div>
    </div>
</div>


<!-- Button DeleteMenuModal trigger modal -->
@*<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#DeleteMenuModal"></button>*@

<!-- Modal DeleteMenuModal -->
<div class="modal fade" id="DeleteVideoModal" tabindex="-1" role="dialog" aria-labelledby="DeleteVideoModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="DeleteVideoModalLabel">@Kaisokku_Sales.Resources.Resource.DeleteVideo</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body DeleteVideocontent">
                @Kaisokku_Sales.Resources.Resource.AreyousureyouwanttodeleteVideo

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">@Kaisokku_Sales.Resources.Resource.No</button>
                <button type="button" class="btn btn-primary btndeletevideo btn-danger">@Kaisokku_Sales.Resources.Resource.Yes</button>
            </div>
        </div>
    </div>
</div>
<div class="modal fade" id="SucMSG" tabindex="-1" role="dialog" aria-labelledby="" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">

            <div class="modal-body ">
                <label id="MSG"></label>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-dismiss="modal">OK</button>
            </div>
        </div>
    </div>
</div>

@section ScriptSection
{
    <script>
        (function(window){
	window.htmlentities = {
		/**
		 * Converts a string to its html characters completely.
		 *
		 * param {String} str String with unescaped HTML characters
		 */
		encode : function(str) {
			var buf = [];

			for (var i=str.length-1;i>=0;i--) {
				buf.unshift(['&#', str[i].charCodeAt(), ';'].join(''));
			}

			return buf.join('');
		},
		/**
		 * Converts an html characterSet into its original character.
		 *
		 * param {String} str htmlSet entities
		 **/
		decode : function(str) {
			return str.replace(/&#(\d+);/g, function(match, dec) {
				return String.fromCharCode(dec);
			});
		}
	};
})(window);
            function RefreshDataTable() {
                //debugger;
                $('#VideoGrid').DataTable().ajax.reload(null, false);
                $("#VideoGrid").DataTable().page('first').draw('page');
            //mApp.unblock('#dltpopup .modal-content');
            //mApp.unblockPage();
        }
        $(document).ready(function () {
            //debugger;
            $("#VideoGrid").DataTable({

                "processing": true, // for show progress bar
                "serverSide": false, // for process server side
                "filter": true, // this is for disable filter (search box)
                "orderMulti": false, // for disable multiple column at once
                "pageLength": 10,
                language: {
                    searchPlaceholder: "Title"
                },

                "ajax": {

                     "url":  "@Url.Action("LoadVideoListData", "Home")",
                    "type": "POST",
                    "datatype": "json"
                },

                "columnDefs":
                    [{
                        "targets": [0],
                        "visible": false,
                        "searchable": true,
                        "orderable": true
                    },
                    {
                        "targets": [1],
                        //"visible": true,
                        "searchable": true,
                        "orderable": false
                    },
                    {
                        "targets": [2],
                        "searchable": false,
                        "orderable": true
                    },
                    {
                        "targets": [3],
                        "searchable": false,
                        "orderable": false
                    },
                    {
                        "targets": [4],
                        "searchable": false,
                        "orderable": false
                    },
                    {
                        "targets": [5],
                        "searchable": false,
                        "orderable": false
                    },
                    {
                            "targets": [6],
                            "searchable": false,
                            "orderable": false
                        },
                        {
                            "targets": [7],
                            "searchable": false,
                            "orderable": false
                        },
                        {
                            "targets": [8],
                            "searchable": false,
                            "orderable": false
                        }

                    ],


                "columns": [
                    { "data": "MediaID", "name": "MediaID", "autoWidth": true },
                    { "data": "Title", "name": "Title", "autoWidth": true },
                    { "data": "Description", "name": "Description", "autoWidth": true },
                   // { "data": "Filename", "name": "Filename", "autoWidth": true },

                    { "data": "IPaddress", "name": "IPaddress", "autoWidth": true },
                    { "data": "Viewcount", "name": "Viewcount", "autoWidth": true },
                    { "data": "UserTypeName", "name": "UserTypeName", "autoWidth": true },

                    {
                        //"render": function (data, type, row, meta) { return "<a class='btn btn-info' href='javascript:void(0);' onclick=EditVideo('" + row.MediaID + "');>@Kaisokku_Sales.Resources.Resource.Edit</a>"; }
                        "render": function (data, type, row, meta) { return "<a class='' href='javascript:void(0);' onclick=EditVideo('" + row.MediaID + "');><span  class='glyphicon glyphicon-pencil' aria-hidden='true'></a>"; }
                    },
                    {
                        //"render": function (data, type, row, meta) { return "<a class='' href='javascript:void(0);' onclick=ViewVideo('" + row.MediaID + "');>@Kaisokku_Sales.Resources.Resource.View</a>"; }
                        "render": function (data, type, row, meta) { return "<a class='' href='javascript:void(0);' onclick=ViewVideo('" + row.MediaID + "');><span  class='glyphicon glyphicon-eye-open' aria-hidden='true'></a>"; }
                    },
                    {
                        data: null, render: function (data, type, row) {
                            //return "<a href='#' class='btn btn-danger' onclick=DeleteVideoConfirmation('" + row.MediaID + "'); >@Kaisokku_Sales.Resources.Resource.Delete</a>";
                            return "<a href='#' class='' onclick=DeleteVideoConfirmation('" + row.MediaID + "'); ><span  class='glyphicon glyphicon-trash' aria-hidden='true'></a>";
                        }
                    },

                ]

            });
            $('.btncreatemenu').on('click', function () {
              //  $('.editVideocontent').html();
              //  $('#EditVideoModal').modal('show');
                //debugger;
                $.ajax({
                url: '@Url.Action("CreateVideoListData", "Home")',
                type: 'GET',
                success: function (result) {
                    //debugger;
                    $('.createVideocontent').html(result);
                    $('#CreateVideoModal').modal('show');
                     },
                error: function (error) {
                    console.log(error);
                }
            });
            });

            $("#ViewVideoModal").on('hidden.bs.modal', function (e) {
                $("#myVideoOne").remove();
                //$("#video").attr("src", ""); // Remove the video source.
                //myVideoOne
                //$("#ViewVideoModal source").attr("src", $("#ViewVideoModal source").attr("src"));
            });
            $("#EditVideoModal").on('hidden.bs.modal', function (e) {
                $("#myVideoOne").remove();
                //$("#video").attr("src", ""); // Remove the video source.
                //myVideoOne
                // $("#EditVideoModal source").attr("src", $("#EditVideoModal source").attr("src"));
            });

            $('.btnsavemenu').on('click', function () {
                //debugger;
                var x = 0;
                document.getElementById("title").innerHTML = "";
                document.getElementById("des").innerHTML = "";
                document.getElementById("upload").innerHTML = "";

                if ($('#EditVideoId').val() == "") {
                    document.getElementById("demo").innerHTML = "Please enter ID";
                }
                if ($('#EditVideoTitle').val() == "") {
                    //document.getElementById("demo").innerHTML = "Please enter Title";
                    x = x + 1;
                    document.getElementById("title").innerHTML = "Please enter Title";

                }
                if ($('#EditVideoDesc').val() == "") {
                    //document.getElementById("demo").innerHTML = "Please enter Description";
                    x = x + 1;
                    document.getElementById("des").innerHTML = "Please enter Description";
                }
                if ($("#EdtiVideoFile").get(0).files.length != 0) {

                    var allowedExtensions = '@System.Configuration.ConfigurationManager.AppSettings["AllowedExtensions"].ToString()';
                    y = 0;
                    var AEarray = allowedExtensions.split(',');
                    extension = $("#EdtiVideoFile").get(0).files[0].name.split('.')[1].toLowerCase();
                    for (var j = 0; j < AEarray.length; j++) {
                        if (AEarray[j] == extension) {
                            y = 1;
                            break;
                        }


                        //alert(array[j])
                        //var sCurExtension = _validFileExtensions[j];
                        //if (sFileName.substr(sFileName.length - sCurExtension.length, sCurExtension.length).toLowerCase() == sCurExtension.toLowerCase()) {
                        //    blnValid = true;
                        //    break;
                        //}
                    }

                    if (y == 0) {
                        x = x + 1;
                        document.getElementById("upload").innerHTML = "Invalid file Format. Only " + allowedExtensions + " are allowed.";
                    }

                }



                    if (x == 0) {
                    var VideoId = $('#EditVideoId').val();
                    var VideoTitle = $('#EditVideoTitle').val();
                    var VideoDesc = $('#EditVideoDesc').val();
                    var updateVideoFile = $('#EdtiVideoFile').get(0).files;
                    var ExistingVideoName = $('#SelectVideo option:selected').text();
                        var UserType = $('#UserType option:selected').val();

                    var fileData = new FormData();

                    for (var i = 0; i < updateVideoFile.length; i++) {
                        fileData.append("EdtiVideoFile", updateVideoFile[i]);
                    }
                    fileData.append('VideoId', VideoId)
                    fileData.append('VideoTitle', VideoTitle)
                    fileData.append('VideoDesc', VideoDesc)
                    fileData.append('ExistingVideoName', ExistingVideoName)
                        fileData.append('UserType', UserType)
                    $('#EditVideoModal').modal('hide');
                    $.ajax({
                        url: '@Url.Action("SaveVideoListData", "Home")',
                        type: 'POST',
                        contentType: false,
                        processData: false,
                        data: fileData,
                        success: function (result) {

                            if (result.errorcode == 0) {
                               // alert('@Kaisokku_Sales.Resources.Resource.VideoUpdatedSuccessfully');

                                RefreshDataTable()
                                $('#MSG').text("Video Updated Successfully")
                                $("#SucMSG").modal('show');

                            }
                            else {
                               // alert('@Kaisokku_Sales.Resources.Resource.VideoUpdationFailed');
                                $('#MSG').text("Video Failed to Update")
                                $("#SucMSG").modal('show');
                            }
                        },
                        error: function (error) {
                            console.log(error);
                        }

                    });
                    }


            });



            $('.btnscreateavemenu').on('click', function () {
                //debugger;
                var x = 0;
                document.getElementById("title").innerHTML = "";
                document.getElementById("des").innerHTML = "";
                document.getElementById("upload").innerHTML = "";

                if ($('#CreateVideoTitle').val().trim() == "") {
                    x = x + 1;
                    document.getElementById("title").innerHTML = "Please enter Title";
                }
                if ($('#CreateVideoDesc').val().trim() == "") {
                    x = x + 1;
                    document.getElementById("des").innerHTML = "Please enter Description";
                }
                if ($("#CreateVideoFile").get(0).files.length == 0) {
                    x = x + 1;
                    document.getElementById("upload").innerHTML = "Please upload Video File";
                }
                else {
                    var allowedExtensions = '@System.Configuration.ConfigurationManager.AppSettings["AllowedExtensions"].ToString()';
                    //allowedExtensions = decodeHTML(allowedExtensions);
                    //alert($("#CreateVideoFile").get(0).files[0].name);
                    //var decod = htmlentities.decode(allowedExtensions);
                    y = 0;
                    var AEarray = allowedExtensions.split(',');
                    //decod = decod.replace(/'/g, '"');
                    //outputfile = $("#CreateVideoFile").get(0).files[0].name;
                    extension = $("#CreateVideoFile").get(0).files[0].name.split('.')[1].toLowerCase();
                    //alert($("#CreateVideoFile").get(0).files[0].name);
                    //alert(outputfile);
                    //alert(extension);
                    //alert(allowedExtensions);
                    //alert(decod);
                    for (var j = 0; j < AEarray.length; j++) {
                        if (AEarray[j] == extension) {
                            y = 1;
                            break;
                        }


                        //alert(array[j])
                        //var sCurExtension = _validFileExtensions[j];
                        //if (sFileName.substr(sFileName.length - sCurExtension.length, sCurExtension.length).toLowerCase() == sCurExtension.toLowerCase()) {
                        //    blnValid = true;
                        //    break;
                        //}
                    }

                    if (y == 0) {
                        x = x + 1;
                        document.getElementById("upload").innerHTML = "Invalid file Format. Only " + allowedExtensions + " are allowed.";
                    }

                }


                if (x == 0) {
                    var VideoTitle = $('#CreateVideoTitle').val();
                    var VideoDesc = $('#CreateVideoDesc').val();
                    //var updateVideoFile = $('#CreateVideoFile').val();
                    var updateVideoFile = $("#CreateVideoFile").get(0).files;
                    var UserType = $('#UserType option:selected').val();
                    var fileData = new FormData();

                    for (var i = 0; i < updateVideoFile.length; i++) {
                        fileData.append("CreateVideoFile", updateVideoFile[i]);
                    }
                    fileData.append('VideoTitle', VideoTitle)
                    fileData.append('VideoDesc', VideoDesc)
                    fileData.append('UserType', UserType)
                    $('#CreateVideoModal').modal('hide');
                    $.ajax({
                        url: '@Url.Action("SaveCreateVideoListData", "Home")',
                        type: 'POST',
                        contentType: false,
                        processData: false,
                        data: fileData,
                        success: function (result) {

                            if (result.errorcode == 0) {
                                //alert('@Kaisokku_Sales.Resources.Resource.VideoCreatedSuccessfully');
                                RefreshDataTable();
                                $('#MSG').text("Video Created Successfully")
                                $("#SucMSG").modal('show');
                            }
                            else {
                               // alert('Video Creation Failed due to : ' + result.errormessage);
                                $('#MSG').text('Video Creation Failed due to : ' + result.errormessage)
                                $("#SucMSG").modal('show');
                            }
                        },
                        error: function (error) {
                            console.log(error);
                        }

                    });
                }
                @*if ($('#CreateVideoTitle').val() == "" && $('#CreateVideoDesc').val() == "" && $("#CreateVideoFile").get(0).files.length == 0) {
                    document.getElementById("title").innerHTML = "Please enter Title";
                    document.getElementById("des").innerHTML = "Please enter Description";
                    document.getElementById("upload").innerHTML = "Please upload Video File";
                }
                else if ($('#CreateVideoTitle').val() != "" && $('#CreateVideoDesc').val() == "" && $("#CreateVideoFile").get(0).files.length == 0) {
                    document.getElementById("title").innerHTML = "";
                    document.getElementById("des").innerHTML = "Please enter Description";
                    document.getElementById("upload").innerHTML = "Please upload Video File";
                }
                else if ($('#CreateVideoTitle').val() != "" && $('#CreateVideoDesc').val() != "" && $("#CreateVideoFile").get(0).files.length == 0) {
                    document.getElementById("title").innerHTML = "";
                    document.getElementById("des").innerHTML = "";
                    document.getElementById("upload").innerHTML = "Please upload Video File";
                }
                else if ($('#CreateVideoTitle').val() == "" && $('#CreateVideoDesc').val() == "" && $("#CreateVideoFile").get(0).files.length != 0) {
                    document.getElementById("title").innerHTML = "Please enter Title";
                    document.getElementById("des").innerHTML = "Please enter Description";
                    document.getElementById("upload").innerHTML = "";
                }
                else if ($('#CreateVideoTitle').val() != "" && $('#CreateVideoDesc').val() == "" && $("#CreateVideoFile").get(0).files.length != 0) {
                    document.getElementById("title").innerHTML = "";
                    document.getElementById("des").innerHTML = "Please enter Description";
                    document.getElementById("upload").innerHTML = "";
                }
                else if ($('#CreateVideoTitle').val() == "" && $('#CreateVideoDesc').val() != "" && $("#CreateVideoFile").get(0).files.length != 0) {
                    document.getElementById("title").innerHTML = "Please enter Title";
                    document.getElementById("des").innerHTML = "";
                    document.getElementById("upload").innerHTML = "";
                }
                else if ($('#CreateVideoTitle').val() == "" && $('#CreateVideoDesc').val() != "" && $("#CreateVideoFile").get(0).files.length == 0) {
                    document.getElementById("title").innerHTML = "Please enter Title";
                    document.getElementById("des").innerHTML = "";
                    document.getElementById("upload").innerHTML = "Please upload Video File";
                }

                else {
                    var VideoTitle = $('#CreateVideoTitle').val();
                    var VideoDesc = $('#CreateVideoDesc').val();
                    //var updateVideoFile = $('#CreateVideoFile').val();
                    var updateVideoFile = $("#CreateVideoFile").get(0).files;
                    var fileData = new FormData();

                    for (var i = 0; i < updateVideoFile.length; i++) {
                        fileData.append("CreateVideoFile", updateVideoFile[i]);
                    }
                    fileData.append('VideoTitle', VideoTitle)
                    fileData.append('VideoDesc', VideoDesc)
                    $('#CreateVideoModal').modal('hide');
                    $.ajax({
                        url: '@Url.Action("SaveCreateVideoListData", "Home")',
                        type: 'POST',
                        contentType: false,
                        processData: false,
                        data: fileData,
                        success: function (result) {

                            if (result.errorcode == 0) {
                                alert('@Kaisokku_Sales.Resources.Resource.VideoCreatedSuccessfully');
                                RefreshDataTable();
                            }
                            else {
                                alert('Video Creation Failed due to : ' + result.errormessage);
                            }
                        },
                        error: function (error) {
                            console.log(error);
                        }

                    });
                }*@
            });



            $('.btndeletevideo').on('click', function () {
                DeleteVideo($('#DeleteVideoId').val());
            });
        });
        function getFile(filePath) {
            return filePath.substr(filePath.lastIndexOf('\\') + 1).split('.')[0];
        }

        function EditVideo(MediaID) {

            $.ajax({
                url: '@Url.Action("GetVideoListDataById", "Home")',
                type: 'POST',
                data: { 'MediaID': MediaID },
                success: function (result) {

                    $('.editVideocontent').html(result);
                    $('#EditVideoModal').modal('show');
                },
                error: function (error) {
                    console.log(error);
                }
            });
        }
        function ViewVideo(MediaID) {

            $.ajax({
                url: '@Url.Action("ViewVideoListDataById", "Home")',
                type: 'POST',
                data: { 'MediaID': MediaID },
                success: function (result) {

                    //$('#videofiles').empty();
                    //$('#videofiles').append("<option value='0'>--Select--</option>");
                    //$.each(result, function () {
                    //    videofiles.append($("<option></option>").val(this['Value']).html(this['Text']));
                    //});
                    $('.ViewVideocontent').html(result);
                    $('#ViewVideoModal').modal('show');
                },
                error: function (error) {
                    console.log(error);
                }
            });
        }

        function DeleteVideoConfirmation(MediaID) {

            $('#DeleteVideoId').val(MediaID);
             $('#DeleteVideoModal').modal('show');
        }

        function DeleteVideo(MediaID) {
            //debugger;
            $('#DeleteVideoModal').modal('hide');
            $.ajax({
                url: '@Url.Action("DeleteVideoById", "Home")',
                type: 'POST',
                data: { 'MediaID': MediaID },
                success: function (result) {
                    if (result.errorcode == 0) {

                       // alert('@Kaisokku_Sales.Resources.Resource.VideoDeletedSuccessfully');
                        RefreshDataTable();
                        $('#MSG').text("@Kaisokku_Sales.Resources.Resource.VideoDeletedSuccessfully")
                        $("#SucMSG").modal('show');
                    }
                    else {
                       // alert('@Kaisokku_Sales.Resources.Resource.VideoDeletionFailed');
                         $('#MSG').text("@Kaisokku_Sales.Resources.Resource.VideoDeletionFailed")
                        $("#SucMSG").modal('show');
                    }
                },
                error: function (error) {
                    console.log(error);
                }
            });
        }
    </script>
}
